# 开发依赖:代码风格、格式化、类型检查、测试。安装:pip install -r requirements-dev.txt
-r requirements.txt
ruff>=0.4
black>=24.0
mypy>=1.8          # 静态类型检查(配置见 pyproject.toml [tool.mypy])
pytest>=8.0        # 离线单测(tests/,零 API 成本)
